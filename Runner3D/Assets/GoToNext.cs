using UnityEngine;
using System.Collections;

public class GoToNext : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        /*if (Application.GetStreamProgressForLevel( "ini" ) == 1 ) {
            Application.LoadLevel( "ini" );
        }*/
	}
}
